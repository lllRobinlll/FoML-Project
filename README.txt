Use:
- our code for the final models is in "final_models/.../my_agent"
- models can be tested and trained as in the final_project.pdf in moodle explained

Hyperparameter:
- Hyperparameters can be changed in train.py in setup_training() #Hyperparameter
- Switching between the 3 possible models can be done in callback.py in Setup()

Graphs:
- run plotGraphs.py to get a plot of the rewards of the model throughout the training